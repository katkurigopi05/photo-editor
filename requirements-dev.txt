# Development/test dependencies (headless-runnable parts of the project).
# Install with: pip install -r requirements-dev.txt
-r requirements.txt
-r requirements-cv.txt
-r requirements-video.txt
pytest>=8.0
