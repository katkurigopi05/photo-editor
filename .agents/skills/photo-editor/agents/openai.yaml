interface:
  display_name: "Photo Editor"
  short_description: "Repo-specific patterns and workflows for photo-editor"
  default_prompt: "Use the photo-editor repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
